#include <stdio.h>

int main()
{
    // This is a programme to find the area of a square
    float side;
    printf("Enter length of side of square here ");
    scanf("%f", &side);
    printf("Area of square is : %f", side * side);
    return 0;
}