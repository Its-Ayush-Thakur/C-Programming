#include <stdio.h>
int main()
{
    // Lets check what's the size of datatypes in my machine
    printf("%d \n", sizeof(int));
    printf("%d \n", sizeof(float));
    printf("%d \n", sizeof(char));
    return 0;
}